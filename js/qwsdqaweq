for (let index = 0; index < users.length; index++) {
        if (email == users[index][0]){
            valEmail= true;
            storIndex= index;

            if(password=users[index][1]){
               console.log("here2")
            }
        }